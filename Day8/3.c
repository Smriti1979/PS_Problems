#include<stdio.h>
void main()
{
    int n,a,sum=0;
    printf("Enter the number : ");
    scanf("%d",&n);
    while()
    {
         while(n--)
        {
            a=n%10;
            sum+=a;
            n=n/10;
        }

    }
    printf("sum = %d",sum);
}
